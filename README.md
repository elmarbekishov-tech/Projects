# 🎓 Учебный Telegram-бот (College Support Bot)

Бот для подачи заявок в техническую поддержку колледжа. Позволяет студентам регистрироваться и отправлять заявки, а администраторам — отвечать на них.

## 🛠 Технологии
* **Язык:** Python 3.10+
* **Фреймворк:** aiogram 3.x
* **База данных:** SQLite + SQLAlchemy (Async)
* **Конфигурация:** python-dotenv

## 🚀 Установка и запуск

1.  **Клонируйте проект** или скачайте архив.
2.  **Создайте виртуальное окружение:**
    ```bash
    python -m venv .venv
    ```
3.  **Активируйте окружение:**
    * Windows: `.venv\Scripts\activate`
    * Mac/Linux: `source .venv/bin/activate`
4.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```
5.  **Настройка:**
    Создайте файл `.env` и укажите токен и ID админа:
    ```env
    BOT_TOKEN=ваш_токен
    ADMIN_IDS=12345678,87654321
    ```
6.  **Запуск:**
    ```bash
    python bot.py
    ```

## 📂 Структура проекта
* `handlers/` — Обработчики команд (админ/юзер)
* `database/` — Работа с БД (модели, запросы)
* `keyboards/` — Кнопки меню
* `states/` — Машины состояний (FSM)
* `logs/` — Логи работы бота

## 👤 Автор
Студент 3 курса: [Элмар]